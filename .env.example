# Advisory mode: "mock" (default, no API key needed) or "live" (requires ANTHROPIC_API_KEY)
ADVISORY_MODE=mock

# Required only when ADVISORY_MODE=live
# ANTHROPIC_API_KEY=sk-ant-...

# Claude model for live advisory (optional, has a sensible default)
# CLAUDE_MODEL=claude-sonnet-4-5-20250929

# SQLite database path (default: gtm_deals.db in working directory)
# GTM_DB_PATH=gtm_deals.db

# API base URL — Streamlit uses this to reach FastAPI
# Change this when deploying Streamlit separately from FastAPI
GTM_API_URL=http://localhost:8000

# Optional API key sent as X-API-Key header
# GTM_API_KEY=
